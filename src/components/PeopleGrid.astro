---
interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<div
  class={`flex overflow-x-auto min-[1200px]:flex-wrap min-[1200px]:items-start min-[1200px]:justify-center ${className ?? ""}`}
>
  <slot />
</div>
