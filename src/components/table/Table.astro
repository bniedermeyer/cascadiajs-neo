---
interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<table
  class:list={[
    "mx-auto w-full max-w-300 min-w-200 table-fixed border-collapse",
    className,
  ]}
>
  <slot />
</table>
